spring:
  datasource:
    url: jdbc:postgresql://caboose.proxy.rlwy.net:44204/${PGDATABASE}?sslmode=require
    username: ${PGUSER}
    password: ${PGPASSWORD}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate